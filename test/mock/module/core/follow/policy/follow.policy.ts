import { FollowPolicy } from '../../../../../../src/modules/@core/follow/policy/follow.policy';
import { mockDeep } from 'jest-mock-extended';

export const mockFollowPolicy = () => mockDeep<FollowPolicy>();
