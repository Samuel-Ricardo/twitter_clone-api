version: '3'

services:
  
  code_health:
    build: 
      context: .
      dockerfile: Dockerfile.ci
    entrypoint: ["ls", "-a", "&&","npm", "run", "code:ci"]
    volumes:
      - .:/home/node/app
    
